
function FileManager(elementid,basedir) {
	this.elementid = elementid;
	this.basedir = basedir;
	this.elem = document.getElementById(elementid);
	this.cursor = 
}

FileManager.prototype.updateDisplay(tree) {
}
